{
  "name": "redis-memoizer",
  "version": "4.0.2",
  "description": "A memoizer using redis as a shared cache, with TTL.",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --reporter spec --timeout 5000"
  },
  "engines": {
    "node": ">=8.0"
  },
  "repository": "",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "redis-lock": "github:genru/redis-lock"
  },
  "devDependencies": {
    "redis": "^4.x",
    "delay2": "^1.0.2",
    "mocha": "^4.0.1",
    "should": "^13.1.3"
  }
}
